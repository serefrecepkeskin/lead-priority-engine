# Development dependencies: linting, typing, testing, notebooks.
# Includes runtime deps via -r so a single `pip install -r requirements-dev.txt`
# bootstraps everything needed to work on the repo.

-r requirements.txt

# Lint / format / type-check
ruff~=0.15
mypy~=1.13
pandas-stubs
types-requests

# Tests
pytest~=8.3
pytest-cov~=6.0
pytest-asyncio~=0.24
httpx~=0.27

# Pre-commit
pre-commit~=4.0
nbstripout~=0.7

# Notebooks
jupyter~=1.1
ipykernel~=6.29
matplotlib~=3.9
seaborn~=0.13
openpyxl~=3.1

# Explainability for Phase 2 notebook (row-level SHAP waterfall + summary).
# Dev-only — production scoring does not call SHAP.
shap~=0.52

# Offline data-generation pipeline (scripts/datagen/). Not needed at runtime —
# the API never calls Azure OpenAI or rewrites the methodology docx.
langchain-openai~=0.2
python-docx~=1.1
tqdm~=4.66
